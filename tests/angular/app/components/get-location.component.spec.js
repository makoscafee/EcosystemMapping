ngDescribe({
    name: 'Test get-location component',
    modules: 'app',
    element: '<get-location></get-location>',
    tests: function (deps) {

        it('basic test', () => {
            //
        });
    }
});
