ngDescribe({
    name: 'Test message component',
    modules: 'app',
    element: '<message></message>',
    tests: function (deps) {

        it('basic test', () => {
            //
        });
    }
});
