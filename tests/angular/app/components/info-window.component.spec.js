ngDescribe({
    name: 'Test info-window component',
    modules: 'app',
    element: '<info-window></info-window>',
    tests: function (deps) {

        it('basic test', () => {
            //
        });
    }
});
