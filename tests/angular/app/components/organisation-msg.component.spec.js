ngDescribe({
    name: 'Test organisation-msg component',
    modules: 'app',
    element: '<organisation-msg></organisation-msg>',
    tests: function (deps) {

        it('basic test', () => {
            //
        });
    }
});
