ngDescribe({
    name: 'Test SidemenuDataService',
    modules: 'app',
    inject: 'SidemenuDataService',
    tests: function (deps) {

        it('basic test', () => {
            //
        });
    }
});
