ngDescribe({
    name: 'Test OrganizationService',
    modules: 'app',
    inject: 'OrganizationService',
    tests: function (deps) {

        it('basic test', () => {
            //
        });
    }
});
