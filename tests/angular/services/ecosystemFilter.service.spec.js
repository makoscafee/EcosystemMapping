ngDescribe({
    name: 'Test EcosystemFilterService',
    modules: 'app',
    inject: 'EcosystemFilterService',
    tests: function (deps) {

        it('basic test', () => {
            //
        });
    }
});
