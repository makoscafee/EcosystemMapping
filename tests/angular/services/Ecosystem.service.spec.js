ngDescribe({
    name: 'Test EcosystemService',
    modules: 'app',
    inject: 'EcosystemService',
    tests: function (deps) {

        it('basic test', () => {
            //
        });
    }
});
